registry=https://registry.npmjs.org/

# Hard deadline per HTTP request (ms) — prevents infinite hang on EOF
fetch-timeout=90000

# Retry settings
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=90000

# Suppress noise in CI/Docker logs
progress=false
audit=false
fund=false
